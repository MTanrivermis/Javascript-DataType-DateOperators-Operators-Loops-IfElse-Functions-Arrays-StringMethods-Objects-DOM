const points = [70, 75, 25, 35, 10, 80, 27]; // değişkeni oluşturduk.

points.push(90);  // buradaki puanlara yeni bir puan eklemek için push kullanılır. değişkene ekleme yapıyor yani.
console.log(points);

points.pop(); // en sondaki değeri çıkartarak kalan değeri gösterir.
console.log(points);

points.pop();
console.log(points);

